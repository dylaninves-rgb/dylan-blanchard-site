/* Site settings — edit these two values when you go live.
   Leave blank until you have them; the site still works for preview. */
window.SITE = {
  /* Your live address, no trailing slash.
     Examples: "https://dylanblanchard.com"
               "https://something.netlify.app"
     After setting this, also run:  python3 tools/set_site_url.py
     so social previews (Open Graph) use absolute image URLs. */
  url: "https://gbmisa.github.io/dylan-blanchard-site",

  /* Optional. Free form inbox at https://formspree.io — paste only the form id
     (the bit after /f/), e.g. "xpwgkqyz".
     If you deploy on Netlify instead, leave this blank: the form uses
     Netlify Forms automatically (data-netlify on contact.html). */
  formspreeId: ""
};
