/*
  One object per painting. To add a new piece, copy an object below,
  fill it in, and drop the image file in images/artwork/ — no other
  file needs to change.

  imageWidth / imageHeight are the real pixel dimensions of the file at
  `image` — used to reserve layout space before the image loads.
*/
const ARTWORKS = [
];
